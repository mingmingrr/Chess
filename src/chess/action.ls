/*
require! './conditions.js' : Cond
require! './position.js' : Pos

export attack = () ->

export move = () ->

export take = (action) ->
	action with do
		conditions:
			(not) . Cond.team (==)
			...action.conditions ? []
		units:
			...action.acts ? []

export swap = () ->

export yank = () ->

export driveby = () ->

export promote = () ->

*/
